import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-nest-crud',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
